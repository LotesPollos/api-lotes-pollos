export const queriestotalgastos = {
    getConsultaTotalGastos : 
"select CASE WHEN sum(vrd.Sumat) IS NULL THEN  0 ELSE sum(vrd.Sumat) END gt from (select SUM(gv.Cantidad * gv.Precio) Sumat from Lotes inner join Lote_venta lv on  lv.Id_ref_lote =  Id_lote INNER JOIN  Gastos_ventas gv ON  gv.Id_ref_lote_venta = lv.Id_lote_venta_total WHERE  year(Fecha_gasto) = @Year And Estado_lote = 'Terminado'   group by Id_lote Union all SELECT SUM(gp.Cantidad*gp.Precio)  Sumat from Lotes inner join Lote_produccion lp on  lp.Id_ref_lote =  Id_lote inner JOIN  Gastos_produccion gp ON  gp.Id_ref_lote_produccion = lp.Id_lote_produccion WHERE   year(Fecha_gasto) = @Year And Estado_lote = 'Terminado'  group by Id_lote) vrd",
}